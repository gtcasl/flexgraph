#include <iostream>
#include <fstream>
#include <unistd.h>
#include "test.h"
//#include "vcache.h"

using namespace spmv;
//using namespace spmv::accelerator;

int main(int argc, char **argv) {

  /*ch_device<spmv_vertex_cache<ch_bit4, 8, 8, 2>> device;
  ch_simulator sim(device);
  ch_tick t = sim.reset(0);
  t = sim.step(t);
  int ret = 1;
  assert(!!device.io.enq.ready);*/

  return 0;
}
